frameworkVersion: ">=1.2.1"

service: tensorflow-lambda-gan

provider:
  name: aws
  runtime: python2.7
  stage: dev
  region: us-west-1

functions:
  predict:
    handler: handler.predict
    events:
      - http:
          path: /predict
          method: get